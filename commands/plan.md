---
description: Create a detailed Angular implementation plan for a feature, refactor, or architectural change.
allowed_tools: ["Read", "Grep", "Glob"]
---

Invoke the `planner` agent.

Analyze the current Angular codebase and the user's request, then produce a complete implementation plan following the planner agent's format:
- Phase-by-phase steps with exact file paths and Angular construct types
- Dependencies between steps
- Testing strategy (TestBed unit tests + E2E)
- Risks and mitigations
- Success criteria

Be specific to this Angular/TypeScript project. Reference actual file paths found in the codebase.
